https://leetcode.com/problems/next-greater-element-i/submissions/1675694933/

class Solution {
public:
    vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {
       //TC: O(N+M), SC: O(N)
        int n= nums1.size(), m= nums2.size();
        vector<int> ans(n);
        stack<int> st;
        unordered_map<int, int> mp;
        for(int i= m-1; i>=0; i--){
            
                while(!st.empty() && st.top()<= nums2[i]){
                    st.pop();
                }
                if(st.empty()){
                    mp[nums2[i]]=-1;
                }
                else{
                     mp[nums2[i]]= st.top();
                }
            
            st.push(nums2[i]);
        }
        for(int i=0; i<n; i++){
            if(mp.find(nums1[i])!= mp.end()){
                ans[i]= mp[nums1[i]];
            }
        }
        return ans;
    }
       //BRUTE
        // int n= nums1.size(), m= nums2.size();
        // int i=0, j=0;
        // vector<int> ans(n,-1);
        // for(int i=0; i<n; i++){
        //     int ele= nums1[i];
        //     for(int j=0; j<m; j++){
        //        if(nums2[j]== ele){
        //            for(int k=j+1; k<m; k++){
        //              if(nums2[k]> nums2[j]){
        //                 ans[i]= (nums2[k]);
        //                 break;
        //              }
        //            }
                   
        //        }
        //     }
        // }
        // return ans;
    
};
