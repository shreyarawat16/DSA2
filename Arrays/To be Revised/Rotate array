https://leetcode.com/problems/rotate-array/submissions/1767336770/

class Solution {
public:
    void rotate(vector<int>& nums, int k) {
        //  int n= nums.size();
        //  k=k%n;
        //  if(k==n){
        //     return;
        //  }
        //  if(k==0){
        //     return;
        //  }
        //  vector<int> arr;
        //  for(int i=n-k; i<n; i++){
        //     arr.push_back(nums[i]);
        //  }
        //  for(int i=0; i<=n-k-1; i++){
        //     arr.push_back(nums[i]);
        //  }
        //  int j=0;
        //  for(int i=0; i<n; i++){
        //     nums[i]= arr[j];
        //     j++;
        //  }
        //Optimal
        int n= nums.size();
        k=k%n;
        reverse(nums.begin(), nums.begin()+k+1);
        reverse(nums.begin()+k+1, nums.end());
        reverse(nums.begin(), nums.end());
    }
};
