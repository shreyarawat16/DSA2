https://leetcode.com/problems/check-if-array-is-sorted-and-rotated/submissions/1766281034/

class Solution {
public:
    bool check(vector<int>& nums) {

    //BRUTE. TC: O(N+N+N)= O(N), SC: O(N)
    //     int n= nums.size();
    //     int ind=-1;
    //     for(int i=0; i< nums.size()-1; i++){ 
    //         if(nums[i]> nums[i+1]){
    //             ind=i+1;
    //             break;
    //         }
    //     }
    //     if(ind==-1){
    //         return true;
    //     }
    //    vector<int> arr;
    //    for(int i=ind; i<n; i++){
    //      arr.push_back(nums[i]);
    //    }
    //    for(int i=0; i<ind; i++){
    //      arr.push_back(nums[i]);
    //    }
    //    for(int i=1; i<n; i++){
    //       if(arr[i] < arr[i-1]){
    //          return false;
    //       }
    //    }
    //    return true;
      //Optimal, TC: O(N), SC: O(1)
        int cnt=0;
        int n= nums.size();
        for(int i=0; i<n-1; i++){
          if(nums[i]> nums[i+1]){
            cnt++;
          }
        }
          if(nums[n-1]> nums[0]){
            cnt++;
          }
       return cnt<=1;
    }
};
