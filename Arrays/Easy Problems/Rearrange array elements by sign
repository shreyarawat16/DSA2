https://leetcode.com/problems/rearrange-array-elements-by-sign/submissions/1768955351/

class Solution {
public:
    vector<int> rearrangeArray(vector<int>& nums) {
        int n= nums.size();
        vector<int> ans(n);
        int odd=0, even=1;
        for(int i=0; i<n; i++){
            if(nums[i]>0){
                ans[odd]= nums[i];
                odd+=2;
            }
            else if(nums[i]<0){
                ans[even]=nums[i];
                even+=2;
            }
        }
        return ans;
    }
};
