https://leetcode.com/submissions/detail/1712464867/

class Solution {
public:
static bool cmp(pair<int,int> a, pair<int, int> b){
    if(a.first < b.first){
        return true;
    }
    else return false;
}
    vector<int> twoSum(vector<int>& nums, int target) {
        //BRUTE TC: O(N*N) SC: O(1)
        // int n= nums.size();
        // vector<int> ans;
        // for(int i=0; i<n-1; i++){
        //     int sum=0;
        //     for(int j=i+1; j<n; j++){
        //       sum= nums[i]+nums[j];
        //       if(sum== target){
        //          ans.push_back(i);
        //          ans.push_back(j);
        //          break;
        //       }
        //     }
        // }
        // return ans;

        //2 POINTERS
        //TC: O(N* NLOGN *N), SC:O(N)
        // vector<pair<int, int>> v;
        // for(int i=0; i<nums.size(); i++){ //O(N)
        //       v.push_back({nums[i],i});
        // }
        
        // sort(v.begin(), v.end(), cmp); //O(NLOGN)
        // vector<int> ans;
        // int i=0, j= v.size()-1;
        // int sum=0;
        // while(i<=j){  //O(N)
        //  sum= v[i].first + v[j].first;
        //  if(sum== target){
        //      ans.push_back(v[i].second);
        //      ans.push_back(v[j].second);
        //      break;
        //  }
        //  else if(sum< target){
        //     i++;
        //  }
        //  else{
        //     j--;
        //  }
        // }
        // return ans;

        //OPTIMAL- HASHMAP
        //TC: O(N*N), SC:O(N)
        unordered_map<int, int> mp;
        vector<int> ans;
        for(int i=0; i<nums.size(); i++){ //O(N)
            if(mp.find(target- nums[i])!= mp.end()){ //O(N) worst case
                ans.push_back(mp[target-nums[i]]);
                ans.push_back(i);
                break;
            }
            mp[nums[i]]=i;
        }
        return ans;
    }
};
