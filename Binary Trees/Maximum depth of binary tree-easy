https://leetcode.com/problems/maximum-depth-of-binary-tree/submissions/1695352658/

class Solution {
public:
    int maxDepth(TreeNode* root) {
        if(!root) return 0;
        int left= maxDepth(root->left);
        int right= maxDepth(root->right);
        return 1+ max(left, right);
    }
};
