https://leetcode.com/problems/k-closest-points-to-origin/submissions/1690879408/

class Solution {
public:
    vector<vector<int>> kClosest(vector<vector<int>>& points, int k) {
        vector<vector<int>> ans;
        priority_queue<pair<int, int>> pq;
        int n= points.size();
        int m= points[0].size();
        vector<int> zero(2,0);
        for(int i=0; i<n; i++){
            int dist = points[i][0] * points[i][0] + points[i][1] * points[i][1];
              pq.push({dist, i});
              if(pq.size()>k){
                pq.pop();
              }
        }
        while(!pq.empty()){
            int ind =pq.top().second;
            pq.pop();
            ans.push_back(points[ind]);
        }
        return ans;

    }
};
