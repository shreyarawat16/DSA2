https://leetcode.com/problems/kth-smallest-element-in-a-bst/submissions/1741071679/

class Solution {
public:
//Optimal TC: O(N), SC:O(1)+ RECURSIVE STACK SPACE
void solve(TreeNode* root, int& ans, int k, int& cnt){
    if(root==NULL) return;
    //left node right
    solve(root->left, ans, k, cnt);
        cnt++;
        if(cnt==k){
            ans= root->val;
            return;
        }
    solve(root->right, ans, k, cnt);
}
 int kthSmallest(TreeNode* root, int k) {
   int ans=INT_MAX, cnt=0;
   solve(root,ans,k, cnt);
   return ans;
 }
//BRUTE
//TC: O(N), SC:O(N)+ recursive stack space
// void inorderTraversal(TreeNode* root, vector<int>& inorder){
//     if(root==NULL){
//         return;
//     }
//     inorderTraversal(root->left, inorder);
//     inorder.push_back(root->val);
//     inorderTraversal(root->right, inorder);
// }
//     int kthSmallest(TreeNode* root, int k) {
//        vector<int> inorder;
//        inorderTraversal(root, inorder);
//        return inorder[k-1];
//     }
};
